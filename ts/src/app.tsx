import "@/assets/css/app.css";

// so we're missing the nav, the logo, the account profile.
// nav goes on bottom-center
// logo goes top-center for home
// profile goes bottom-right

export function App() {
  return (
    <div className="w-full h-screen flex justify-center items-center">
      {/* normal shit here */}
    </div>
  );
}

export default App;
